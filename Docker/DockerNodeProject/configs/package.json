{
  "name": "docker-sample-project",
  "version": "1.0.0",
  "description": "A sample project to demonstrate Docker with volumes, bind mounts, and Dockerfile.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}

